for child in page.get('/projects').children
  div.row
    h2
      if isActive(child)
        a.active(href=child.url) #{child.title}
      else
        a(href=child.url) #{child.title}
    div #{child.excerpt}